function makeToast() {
    // Simulating preparation of a toast.
    var start = Date.now();
    while((Date.now() - start) < 1000)
    {
    }
    return true;
}

function makeEggs() {
    // Simulating preparation of eggs.
    var start = Date.now();
    while((Date.now() - start) < 1000)
    {
    }
    return true;
}
